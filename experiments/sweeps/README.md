# Sweeps

W&B sweep YAML files live under `configs/sweeps/` and point at `src/cliffordnet/sweeps/train_sweep.py`.

Examples:

```bash
wandb sweep configs/sweeps/hparam.yaml
wandb sweep configs/sweeps/arch.yaml
```
