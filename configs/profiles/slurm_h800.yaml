run:
  tags: [slurm, h800]

runtime:
  devices: auto
  num_nodes: auto

slurm:
  nodes: 1
  gpus_per_node: 8
  gpus_per_task: "1"
  gpu_type: h800
  cpus_per_task: 8
  time: "04:00:00"
  script_dir: ./outputs/slurm
